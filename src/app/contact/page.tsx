import "./page.scss";

const Contact: React.FunctionComponent = (): JSX.Element => {
  return (
    <main className="contact">
      <h1> Contact page </h1>
    </main>
  );
};

export default Contact;
